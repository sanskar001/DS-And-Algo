/* Insertion Sort

- Builds up the sort by gradually creating a large left which is always sorted.

PseudoCode:

- Start by picking the second element in the array
- Now compare the second element with the one before it and swap if necessary.
- Continue to the next element and if it is in the correct order, iterate 
  through the sorted portion(i.e. the left side) to place the element in the correct place.  
- Repeat until the array get sorted.

*/

function insertionSort(arr) {
  return arr;
}

console.log([2, 1, 9, 76, 4]);
